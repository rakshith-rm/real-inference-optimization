# Install ONE stack per venv — do NOT pip install vllm + sglang together (conflicts).
#
# For sglang/ benchmark (default):
#   pip install -r requirements.txt
#
# For vllm/ benchmark, use instead:
#   pip install -r requirements-vllm.txt

requests
huggingface_hub
sglang>=0.4.6
